spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml          # 配置文件的格式
        group: DEFAULT_GROUP          # 配置分组
        namespace: ""                 # 和 discovery 保持一致，空字符串就是 public